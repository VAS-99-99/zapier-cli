{
  "manifest_version": "0.3",
  "name": "zapier-pp-mcp",
  "display_name": "Zapier",
  "version": "0.1.0",
  "description": "Strictly read-only Zapier inspection: verify the connected account, list and search Zaps, inspect historical runs, and diagnose failed steps.",
  "author": {
    "name": "CLI Printing Press"
  },
  "license": "Apache-2.0",
  "server": {
    "type": "binary",
    "entry_point": "bin/zapier-pp-mcp",
    "mcp_config": {
      "command": "${__dirname}/bin/zapier-pp-mcp",
      "args": [],
      "env": {
        "ZAPIER_BASE_URL": "${user_config.zapier_base_url}",
        "ZAPIER_USER_AGENT": "${user_config.zapier_user_agent}"
      }
    }
  },
  "user_config": {
    "zapier_base_url": {
      "type": "string",
      "title": "ZAPIER_BASE_URL",
      "description": "Optional. Sets ZAPIER_BASE_URL for the Zapier MCP server. Per-instance, non-secret setting; not a credential."
    },
    "zapier_user_agent": {
      "type": "string",
      "title": "ZAPIER_USER_AGENT",
      "description": "Optional. Sets ZAPIER_USER_AGENT for the Zapier MCP server. Per-instance, non-secret setting; not a credential."
    }
  },
  "compatibility": {
    "claude_desktop": ">=1.0.0",
    "platforms": [
      "darwin",
      "linux",
      "win32"
    ]
  }
}
